- name: install , run  Nginx
  hosts: web
  become: yes
  tasks: 
  - name: install nginx package
    ansible.builtin.package:
      name: ansible
      state: present
  - name: Run Nginx
    ansible.builtin.service:
      name: ansible
      state: started