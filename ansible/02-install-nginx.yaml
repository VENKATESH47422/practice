- name: install , run  Nginx
  hosts: web
  become: yes
  tasks: 
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Run Nginx
    ansible.builtin.service:
      name: nginx
      state: started